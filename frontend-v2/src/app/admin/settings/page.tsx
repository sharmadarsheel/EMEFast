"use client";
import {useState} from 'react'; import {Save,SlidersHorizontal,Wifi,Clock3} from 'lucide-react';
export default function AdminSettings(){const [sla,setSla]=useState('45');const [poll,setPoll]=useState('4');const [osrm,setOsrm]=useState('https://router.project-osrm.org');const [saved,setSaved]=useState(false);const save=()=>{localStorage.setItem('emefast-settings',JSON.stringify({sla,poll,osrm}));setSaved(true);setTimeout(()=>setSaved(false),1600)};return <div className="max-w-4xl mx-auto px-6 py-8 space-y-6"><div><div className="eyebrow"><SlidersHorizontal size={13}/> NETWORK CONFIGURATION</div><h1 className="text-2xl font-bold mt-2">Admin Settings</h1><p className="text-sm opacity-70 mt-1">Demo controls for response timing, polling and routing configuration.</p></div><section className="v2-card p-6 space-y-5"><label className="settings-row"><span><Clock3 size={16}/><b>Response SLA</b><small>Target hospital response window in seconds</small></span><input value={sla} onChange={e=>setSla(e.target.value)} type="number" min="10" max="300"/></label><label className="settings-row"><span><Wifi size={16}/><b>Live polling interval</b><small>Dashboard refresh interval in seconds</small></span><input value={poll} onChange={e=>setPoll(e.target.value)} type="number" min="2" max="30"/></label><label className="settings-row"><span><SlidersHorizontal size={16}/><b>Routing endpoint</b><small>OSRM-compatible routing service</small></span><input value={osrm} onChange={e=>setOsrm(e.target.value)}/></label><div className="flex justify-end"><button className="primary-action" onClick={save}><Save size={15}/>{saved?'Saved':'Save settings'}</button></div></section></div>}
